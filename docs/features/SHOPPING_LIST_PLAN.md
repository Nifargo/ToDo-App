# План імплементації Shopping List (Список покупок для кількох користувачів)

## Мета
Додати функціонал Shopping List, яким можуть керувати кілька користувачів одночасно з real-time синхронізацією.

## Обрана технологія
- **Firebase Firestore** - для зберігання списку покупок та real-time синхронізації
- **Firebase Authentication** - для авторизації користувачів (вже реалізовано ✅)
- **Firestore `.onSnapshot()`** - для миттєвої синхронізації між користувачами

---

## Фаза 1: Дизайн та структура даних

### 1.1 Firestore структура
```javascript
// Collection: shoppingLists
{
  listId: "auto-generated-id",
  name: "Groceries",
  createdBy: "userId",
  createdAt: timestamp,
  sharedWith: ["userId1", "userId2"],  // Список користувачів з доступом
  items: [
    {
      id: "auto-generated-id",
      name: "Milk",
      quantity: "2L",
      category: "dairy",  // groceries, household, etc.
      isPurchased: false,
      addedBy: "userId",
      addedAt: timestamp,
      purchasedBy: "userId" | null,
      purchasedAt: timestamp | null
    }
  ]
}
```

### 1.2 UI/UX Design
- [ ] Нова вкладка "Shopping" в bottom navigation (🛒)
- [ ] Список покупок з категоріями
- [ ] Кнопка "Add item" для додавання товарів
- [ ] Checkbox для позначення "Purchased"
- [ ] Аватари користувачів біля товарів (хто додав)
- [ ] Кнопка "Share list" для запрошення інших користувачів

---

## Фаза 2: Backend логіка (Firestore)

### 2.1 CRUD операції
- [ ] `createShoppingList(name)` - створити новий список
- [ ] `addItem(listId, item)` - додати товар в список
- [ ] `updateItem(listId, itemId, updates)` - редагувати товар
- [ ] `deleteItem(listId, itemId)` - видалити товар
- [ ] `markAsPurchased(listId, itemId)` - позначити як куплений
- [ ] `shareList(listId, userId)` - поділитися списком з користувачем

### 2.2 Real-time синхронізація
- [ ] Firestore `.onSnapshot()` listener для автоматичного оновлення списку
- [ ] Обробка конфліктів (якщо 2 користувачі редагують одночасно)
- [ ] Offline persistence (можливість працювати без інтернету)

### 2.3 Firestore Security Rules
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /shoppingLists/{listId} {
      // Дозволити читання якщо користувач в sharedWith array або creator
      allow read: if request.auth != null &&
        (resource.data.createdBy == request.auth.uid ||
         request.auth.uid in resource.data.sharedWith);

      // Дозволити запис якщо користувач в sharedWith array або creator
      allow write: if request.auth != null &&
        (resource.data.createdBy == request.auth.uid ||
         request.auth.uid in resource.data.sharedWith);
    }
  }
}
```

---

## Фаза 3: Frontend UI

### 3.1 Shopping List View
- [ ] Створити `shopping-view` HTML секцію
- [ ] Header з назвою списку та кнопкою "Share"
- [ ] Список товарів з категоріями
- [ ] Checkbox для "Purchased"
- [ ] Кнопка "Add new item" (floating action button)

### 3.2 Add/Edit Item Modal
- [ ] Input для назви товару
- [ ] Input для кількості (опціонально)
- [ ] Dropdown для категорії (Groceries, Household, Other)
- [ ] Кнопки "Save" та "Cancel"

### 3.3 Share List Modal
- [ ] Input для email користувача
- [ ] Кнопка "Send invite"
- [ ] Список користувачів з доступом (з можливістю видалити)

---

## Фаза 4: Додаткові фічі

### 4.1 Категорії
- [ ] Іконки для категорій (🥛 dairy, 🍞 bread, 🧴 household)
- [ ] Фільтрація по категоріях
- [ ] Auto-suggest категорій при додаванні

### 4.2 Історія покупок
- [ ] Окрема секція "Purchased items"
- [ ] Можливість повернути товар в список (unmark as purchased)
- [ ] Очищення історії (delete all purchased)

### 4.3 User Collaboration
- [ ] Показувати аватари користувачів біля товарів
- [ ] "Added by [User Name]" під товаром
- [ ] Real-time присутність користувачів (хто зараз переглядає список)

---

## Фаза 5: Тестування

### 5.1 Multi-user тестування
- [ ] Створити список на Desktop
- [ ] Авторизуватися з іншим акаунтом на Mobile
- [ ] Поділитися списком
- [ ] Додати товар на Desktop → перевірити чи з'явився на Mobile (real-time)
- [ ] Позначити як purchased на Mobile → перевірити чи оновилося на Desktop

### 5.2 Offline тестування
- [ ] Додати товари offline
- [ ] Перевірити синхронізацію при відновленні з'єднання

### 5.3 Edge cases
- [ ] Конфлікти при одночасному редагуванні
- [ ] Видалення списку (cascade delete items)
- [ ] Користувач видаляє свій акаунт (cleanup shared lists)

---

## Прогрес

**Загалом:** 0 з ~25 завдань виконано (0%)

- [ ] **Фаза 1: Дизайн та структура даних (2/2)** ⏸️ PLANNED
- [ ] **Фаза 2: Backend логіка (8/8)** ⏸️ PLANNED
- [ ] **Фаза 3: Frontend UI (8/8)** ⏸️ PLANNED
- [ ] **Фаза 4: Додаткові фічі (7/7)** ⏸️ PLANNED
- [ ] **Фаза 5: Тестування (5/5)** ⏸️ PLANNED

---

**Останнє оновлення:** 2025-11-28 (план створено, готові до імплементації!)